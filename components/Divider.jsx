import { styled } from '../stitches.config'

export const Divider = styled('hr', {
  borderColor: 'hsl(var(--nextra-primary-hue) 100% 94%/.1)',
  borderWidth: 0,
  borderBottomWidth: 1,
  marginTop: 24,
  marginBottom: 24,
})
